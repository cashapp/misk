web:
  port: 8080
  idle_timeout: 30000
#  host: let's try something new

data_source_cluster:
  writer:
    type: MYSQL
    username: root
    password: ""
    database: urls
    migrations_path: migrations

endpoint:
  base_url: http://localhost:8080/
