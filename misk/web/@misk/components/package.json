{
  "name": "@misk/components",
  "version": "0.0.2",
  "description": "Microservice Kontainer Common Components",
  "author": "Square/Misk Authors (https://github.com/square/misk/graphs/contributors)",
  "main": "lib/index.js",
  "module": "lib/index.js",
  "engines": {
    "node": ">=7.8.0"
  },
  "files": [
    "lib"
  ],
  "style": "lib/styles.js",
  "repository": {
    "type": "git",
    "url": "git@github.com:square/misk.git"
  },
  "scripts": {
    "build": "npm run lib",
    "build:lib": "npm run lib",
    "dist": "npm run lib",
    "lib": "yarn pre && webpack",
    "lint": "echo no lint",
    "pre": "mkdir -p ../common/node_modules && rm -rf ../common/node_modules && mkdir -p node_modules/@misk/common && cp -r ../common node_modules/@misk",
    "prepare": "yarn run lint && yarn run test && yarn run lib",
    "reinstall": "rm -rf node_modules/; yarn install",
    "test": "echo no tests"
  },
  "dependencies": {
    "@misk/common": "^0.0.2"
  },
  "devDependencies": {
    "@misk/dev": "^0.0.2"
  }
}
