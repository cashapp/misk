web:
  port: 8080
  idle_timeout: 30000

data_source_cluster:
  writer:
    type: MYSQL
    username: root
    password: ""
    database: urls
    migrations_path: migrations

endpoint:
  base_url: http://localhost:8080/

http_clients:
  endpoints:
    for_shortened_urls:
      url: https://localhost/
    web_proxy_interceptor:
      url: http://localhost/