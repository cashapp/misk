
syntax = "proto2";

package misk.hibernate.security;

option java_package = "misk.hibernate";

message SecretColumnContents {
    // The meaning and usage of individual fields can change even if structure doesn't, thus we
    // require a version
    required uint32 version = 1;

    optional bytes payload = 2;
}

