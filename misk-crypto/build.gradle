dependencies {
  compile dep.apacheCommonsCollections4
  compile dep.tink
  compile project(':misk')

  testCompile project(':misk-testing')
}

afterEvaluate { project ->
  project.tasks.dokka {
    outputDirectory = "$rootDir/docs/0.x"
    outputFormat = 'gfm'
  }
}